# License activation route

This route intentionally uses force-dynamic and no-store settings and displays a visible release marker so administrators can confirm that the latest deployment is being served instead of an older cached or immutable Vercel deployment URL.
