name: Superseded macOS desktop v1.0.6 workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'v1.0.6 publishing is disabled. Use publish-macos-v1.0.7.yml.'
